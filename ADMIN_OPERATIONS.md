# 꼬붕봇 관리자 운영 가이드

대상 버전: `0.3.0`  
운영 모드: 3개 내전방 동시 운영, BO3(3판 2선승)

## 1. 시작 전 점검

프로젝트 폴더에서 Discord 로그인 전 자동 점검을 실행합니다.

```text
python preflight.py
```

모든 필수 항목이 `✅`인지 확인한 뒤 봇을 실행합니다. 이 점검은 Discord에 로그인하지 않으며 운영 DB 무결성을 확인하고 새 검증 백업을 생성합니다.

DB 구조에는 스키마 버전이 기록됩니다. 새 버전에서 구조 변경이 필요하면 먼저 `data/backups/schema_before_v버전_*.db`를 생성하고 각 마이그레이션을 한 번만 트랜잭션으로 적용합니다. 실패하면 해당 버전 변경을 롤백하고 봇 시작을 중단합니다.

봇 실행 터미널에서 입력 커서가 돌아오지 않는 것은 정상입니다. 같은 PC에서 `python bot.py`를 다시 실행하면 중복 실행 보호가 이를 차단합니다. 재시작할 때는 기존 터미널에서 `Ctrl+C`로 정상 종료한 뒤 새로 실행합니다.

인터넷 또는 Discord Gateway 연결이 일시적으로 끊기면 봇이 자동 재연결을 시도합니다. `logs/bot.log`에서 `Gateway 연결 끊김`, `자동 재연결 대기`, `Gateway 세션 복구 완료` 순서를 확인합니다. 복구 완료가 장시간 나타나지 않을 때만 정상 종료 후 재시작합니다.

같은 사용자가 같은 슬래시 명령을 반복하면 기본 2초 동안 재실행이 제한됩니다. `/라이엇조회`는 Riot API 보호를 위해 10초 제한을 사용합니다. 서로 다른 명령이나 다른 사용자의 요청은 독립적으로 처리됩니다.

### Windows 로그인 시 자동 실행

PowerShell에서 다음 설치 스크립트를 한 번 실행합니다.

```text
powershell -ExecutionPolicy Bypass -File scripts/install_scheduled_task.ps1
```

등록된 `KkobungBot` 작업은 사용자 로그인 시 숨김 창으로 봇을 시작합니다. 비정상 종료 시 1분 간격으로 최대 10회 재시작하고, 이미 실행 중이면 새 인스턴스를 만들지 않습니다.

설치와 동시에 봇을 시작하려면 `-StartNow`를 추가합니다.

```text
powershell -ExecutionPolicy Bypass -File scripts/install_scheduled_task.ps1 -StartNow
```

자동 실행 등록만 제거할 때는 다음을 사용합니다. 실행 중인 봇 프로세스는 기본적으로 종료하지 않습니다.

```text
powershell -ExecutionPolicy Bypass -File scripts/uninstall_scheduled_task.ps1
```

### 매일 외부 DB 백업

외부 백업은 프로젝트 내부가 아닌 `OneDrive/꼬붕봇_외부백업/`에 저장되며 최근 30개를 유지합니다. 각 파일은 생성 직후 SQLite 무결성 검사를 통과해야 보관됩니다.

한 번 직접 실행:

```text
python external_backup.py
```

매일 오전 3시 자동 실행 등록:

```text
powershell -ExecutionPolicy Bypass -File scripts/install_backup_task.ps1
```

실행 결과는 `logs/backup.log`에서 확인합니다. 자동 백업 작업만 제거할 때는 `scripts/uninstall_backup_task.ps1`을 실행합니다.

봇을 실행하기 전에 `.env`에 다음 값을 준비합니다.

```env
DISCORD_TOKEN=...
RIOT_API_KEY=...
```

- `DISCORD_TOKEN`이 없으면 봇이 시작되지 않습니다.
- `RIOT_API_KEY`가 없으면 Riot API 기능만 사용할 수 없습니다.
- 시작 과정에서 데이터·백업 폴더 쓰기 권한, SQLite 무결성, DB 백업을 자동 확인합니다.

Discord에서 관리자는 먼저 다음 명령을 실행합니다.

```text
/시스템점검
```

점검 결과에는 실행시간, Discord 지연시간, Gateway 끊김·복구 횟수, DB 스키마 버전, 최근 내부·외부 백업 시각, 운영 로그 크기와 마지막 오류 기록 시각이 함께 표시됩니다.

`자동 운영 감시` 항목에서는 감시 작업의 실행 여부, 현재 감지 중인 문제, 마지막 점검·경고·복구 시각을 확인할 수 있습니다.

같은 항목에서 SQLite에 현재 보관 중인 운영 이력 개수와 가장 최근에 저장된 경고·복구 종류 및 시각도 확인할 수 있습니다.

### 운영 이상 자동 경고

봇은 5분마다 DB 무결성, 최근 내부 백업 시각, Discord Gateway 연결 상태를 자동으로 확인합니다.

- DB 무결성 검사 실패
- 내부 백업이 없거나 24시간 이상 오래됨
- 30분 안에 Gateway 연결이 3회 이상 끊김

문제가 감지되면 `ADMIN_IDS`에 등록된 관리자에게 개인 메시지를 보냅니다. `.env`에 `ADMIN_ALERT_CHANNEL_ID=채널ID`를 추가하면 개인 메시지 대신 지정 채널로 전송합니다. 같은 문제는 기본 1시간 동안 반복 전송하지 않습니다.

경고가 전송된 문제가 이후 점검에서 정상으로 돌아오면 초록색 `운영 상태 복구` 알림이 전송됩니다. 복구 알림 전송에 실패하면 다음 5분 점검에서 다시 시도하며, 복구가 확인된 문제는 재알림 제한 기록도 초기화됩니다.

실제로 전송에 성공한 자동 경고와 복구 알림은 SQLite의 `operations_events` 테이블에 저장됩니다. 이 기록은 봇을 재시작해도 유지되며 `/경고테스트`로 보낸 수동 테스트 메시지는 저장하지 않습니다.

봇 시작 시 저장된 이력을 시간순으로 읽어 문제별 마지막 경고·복구 상태를 복원합니다. 재시작 전부터 이어진 장애는 남은 재알림 제한 시간을 유지하며, 재시작 중 정상화된 장애는 첫 자동 점검에서 복구 알림으로 마무리됩니다.

관리자가 `/운영이력`을 실행하면 최근 자동 경고·복구 기록 10개를 최신순으로 확인할 수 있습니다. 결과는 명령을 실행한 관리자에게만 표시됩니다.

`문제` 옵션에서 SQLite DB·내부 DB 백업·Discord Gateway를, `상태` 옵션에서 장애 경고·복구 완료를 선택해 이력을 필터링할 수 있습니다. 두 옵션을 생략하면 전체 기록을 조회합니다.

`개수` 옵션에서 최근 기록 5개·10개·20개 중 표시 개수를 선택할 수 있으며, 생략하면 10개를 표시합니다. 문제·상태 필터와 함께 사용할 수 있습니다.

관리자가 `/운영이력내보내기`를 실행하면 DB에 보관 중인 전체 운영 이력을 CSV 파일로 받을 수 있습니다. 파일은 오래된 기록부터 정렬되며 Excel에서 한글이 깨지지 않는 UTF-8 BOM 형식입니다.

CSV 내보내기에서도 `문제`와 `상태` 옵션으로 기록을 필터링할 수 있습니다. 옵션을 모두 생략하면 보관 중인 전체 이력을 내보냅니다.

운영 이력은 기본적으로 최신 1,000개만 유지됩니다. 새 기록을 저장할 때 초과한 오래된 기록을 같은 DB 트랜잭션 안에서 자동 삭제하며, 보관 개수는 `config.py`의 `OPERATIONS_EVENT_RETENTION_COUNT`에서 변경할 수 있습니다.

설정을 확인할 때 관리자가 `/경고테스트`를 실행하면 실제 전송 대상에 테스트 메시지가 즉시 발송됩니다. 이 테스트는 자동 경고의 재알림 제한 시간에 영향을 주지 않습니다.

수동 테스트 전송은 운영 로그에 `INFO`로 기록되며 실제 자동 장애 경고만 `WARNING`으로 기록됩니다.

확인할 항목:

- SQLite DB가 `정상`인지 확인
- 데이터 폴더와 백업 폴더가 모두 `✅`인지 확인
- 이전 실행에서 남은 `결과 트랜잭션`이나 `pending 복구`가 있는지 확인
- 예상한 내전방 수와 참가자 수가 맞는지 확인

## 2. 최초 방 설정

각 모집용 텍스트 채널에서 방을 생성합니다.

```text
/내전방생성 방이름:<이름>
```

방 상태 확인:

```text
/내전방목록
/내전방정보
```

모든 내전방이 공통으로 사용할 진행 채널에서 실행합니다.

```text
/내전진행채널설정
```

각 모집 채널에서 대기·레드·블루 음성채널을 지정합니다.

```text
/내전음성채널설정
```

팀이나 경기가 존재하는 동안에는 음성채널 설정을 변경할 수 없습니다.

## 3. 일반 BO3 운영 순서

### 모집 시작

해당 내전방의 모집 채널에서 실행합니다.

```text
/내전모집
```

참가자는 모집창 버튼 또는 다음 명령을 사용합니다.

```text
/참가
/참가취소
/명단
```

같은 사용자는 여러 내전방에 동시에 참가할 수 없습니다. 방별 최대 인원은 10명입니다.

### 팀 생성

10명이 모이면 모집창의 `팀 생성` 버튼을 누릅니다. 다시 구성해야 하면 다음 명령을 사용합니다.

```text
/다시뽑기
```

필요한 경우 서로 다른 팀의 두 선수를 교체합니다.

```text
/팀교체
```

팀 생성과 교체 후 선수들은 설정된 팀 음성채널로 이동합니다.

### 경기 시작

팀 결과 화면의 `경기 시작` 버튼 또는 다음 명령을 사용합니다.

```text
/경기시작
```

현재 세트만 취소하고 같은 팀·점수로 다시 시작하려면 다음 명령을 사용합니다.

```text
/세트취소
```

### 경기 결과와 MVP

경기 종료 후 모집 채널에서 실행합니다.

```text
/경기결과
```

1. 관리자가 승리팀을 선택합니다.
2. 경기 참가자들이 승리팀 선수 중 MVP에 투표합니다.
3. 투표 종료 후 경기 기록, 공개 레이팅, Hidden MMR, 시즌 전적과 BO3 점수가 함께 저장됩니다.
4. 한 팀이 2승을 달성하면 시리즈가 종료되고 참가자와 팀 상태가 초기화됩니다.

결과 처리 중에는 경기 시작, 세트 취소, MMR 변경 등 충돌 가능한 작업이 자동으로 대기하거나 거절됩니다.

## 4. 봇 재시작 후 복구

봇은 재시작 시 다음 상태를 방별로 복구합니다.

- 참가자와 현재 팀
- 경기 진행 여부
- BO3 점수와 세트 번호
- 모집·진행·음성채널 설정
- SQLite에 커밋됐지만 JSON 반영 전 종료된 경기 결과

Discord에 `♻️ 내전 상태 복구 안내`가 표시되면 안내된 다음 행동을 따릅니다.

- 참가자만 복구됨: `/내전모집`으로 새 모집창 생성
- 팀이 복구됨: `/경기시작` 또는 기존 경기 절차 계속
- 경기 결과 복구 완료: BO3 점수와 기록을 확인한 뒤 다음 세트 진행

복구 안내는 같은 프로세스에서 Discord가 재연결되어도 한 번만 전송됩니다.

### DB 백업에서 수동 복구

DB 손상이 의심되면 봇이 실행 중인 상태에서 파일을 교체하지 않습니다.

1. 봇 프로세스를 완전히 종료합니다.
2. `data/blooming.db`와 같은 이름의 `-wal`, `-shm` 파일이 있으면 별도 안전 폴더로 함께 옮깁니다.
3. 현재 `data/blooming.db`도 삭제하지 말고 장애 시각이 포함된 이름으로 별도 보관합니다.
4. `data/backups/`에서 장애 발생 전 생성된 가장 최근 `blooming_날짜_시간.db`를 선택합니다.
5. 선택한 백업을 `data/blooming.db`라는 이름으로 복사합니다.
6. 봇을 시작하고 시작 로그의 DB 무결성 검사 완료 여부를 확인합니다.
7. Discord에서 `/시스템점검`을 실행합니다.
8. 참가자, 최근 경기, MMR과 시즌 전적을 표본 조회합니다.

복구가 확인될 때까지 원래 DB와 `-wal`, `-shm` 파일은 보관합니다. 백업 파일 자체는 직접 이동하거나 이름을 바꾸지 말고 복사본을 사용합니다.

## 5. 장애 대응 명령

### 진행 중인 세트만 취소

```text
/세트취소
```

팀과 BO3 점수는 유지됩니다.

### 결과 반영 없이 경기 강제종료

```text
/경기강제종료
```

레이팅과 전적을 반영하지 않고 경기·MVP·pending 복구 상태를 종료합니다.

### 최근 경기 결과 되돌리기

```text
/경기취소
```

가장 최근 경기의 공개 레이팅, Hidden MMR, 배치 경기 수, 시즌 전적, 승패, 연승·연패와 MVP를 트랜잭션으로 복구합니다.

### 경기 레코드만 삭제

```text
/경기기록만삭제 경기번호:<번호>
```

레이팅과 승패는 되돌리지 않고 경기 레코드만 삭제합니다. 현재 방에서 생성된 기록만 삭제할 수 있습니다.

### 내전 전체 종료

```text
/내전종료
```

참가자를 대기 음성채널로 이동한 뒤 참가자, 팀, 경기, BO3, MVP, 트랜잭션과 pending 복구 상태를 모두 초기화합니다.

## 6. 플레이어·MMR 관리

조회:

```text
/mmr확인
/mmr기록
```

수정:

```text
/레이팅수정
/mmr수정
/mmr초기화
```

- 레이팅과 Hidden MMR 입력 범위는 `0~5000`입니다.
- 팀 생성·경기·MVP·결과 처리 중인 선수는 수정할 수 없습니다.
- `/mmr수정`은 공개 레이팅과 배치 경기 수를 유지합니다.
- `/mmr초기화`는 Riot 티어 기준 MMR로 되돌리고 배치 경기 수를 0으로 초기화합니다.

참가자 및 프로필 관리:

```text
/참가자삭제
/프로필삭제
```

경기 처리 중인 선수의 프로필은 삭제할 수 없습니다.

## 7. 테스트 운영

실제 참가자 없이 방 흐름을 확인할 때 사용합니다.

```text
/테스트참가자생성
/테스트초기화
/상대전적테스트
```

권장 확인 순서:

1. 3개 모집 채널에서 각각 테스트 참가자 10명 생성
2. 세 방에서 동시에 팀 생성
3. 경기 시작 후 서로 다른 승리팀 선택
4. MVP 투표와 결과 저장 확인
5. 각 방의 BO3 점수가 섞이지 않는지 `/시스템점검`과 경기 기록으로 확인
6. 테스트가 끝나면 `/테스트초기화` 또는 `/내전종료`

## 8. 위험 명령 주의사항

`/경기결과`의 승리팀 선택창은 방별로 하나만 열립니다. 같은 방에서 명령을 동시에 실행해도 첫 선택창이 잠금 안에서 예약되며, 메시지 전송에 실패하면 예약이 자동 해제됩니다.

MVP 투표는 투표 전용 잠금과 방 상태 잠금을 함께 사용합니다. 동시 클릭도 한 표씩 처리되며, 경기 취소·강제 종료 후 들어온 늦은 버튼은 현재 경기 상태를 다시 확인해 거부합니다.

경기 시작 버튼은 방 잠금 안에서 현재 팀 객체를 다시 확인합니다. 다시뽑기로 팀이 바뀐 뒤의 예전 버튼은 사용할 수 없으며, 경기 진행 또는 MVP 투표 중에는 다시뽑기와 팀교체가 차단됩니다.

각 내전방은 별도의 팀 생성 전용 잠금을 사용합니다. 팀 생성 버튼과 `/다시뽑기`가 동시에 실행되면 첫 요청만 계산하고, 뒤 요청은 기다렸다가 다시 생성하지 않고 즉시 안내됩니다. 다른 내전방의 팀 생성에는 영향을 주지 않습니다.

`/경기강제종료`와 `모집 초기화`는 열려 있던 승리팀 선택창과 MVP 투표창을 즉시 만료시키고 버튼을 비활성화합니다. 이미 시작된 늦은 MVP 종료 콜백도 현재 경기 상태가 종료되었으면 결과 저장을 생략합니다.

방 상태 완전 초기화는 `InhouseRoom.reset_game()`으로 통합되어 참가자·팀·BO3·MVP·결과 트랜잭션·pending 복구 표식을 같은 기준으로 정리합니다. 내전방 삭제는 committed 및 pending 상태가 하나라도 남아 있으면 차단됩니다.

재시작 복구 시 BO3 점수 범위와 세트 번호, 5대5 팀 인원, 중복 선수, 참가자 명단 포함 여부, pending 토큰과 점수 조합을 검증합니다. 숫자가 아닌 사용자 ID, 비어 있거나 잘못된 닉네임 정보는 제거하며, 한 방에 10명을 초과해 저장된 경우 저장 순서상 앞의 10명만 유지합니다. 같은 사용자가 여러 내전방에 저장돼 있으면 먼저 복구된 방에만 남기고 이후 방에서는 제거합니다. 제거된 사용자가 팀에 포함돼 있었다면 해당 팀과 경기 상태도 안전하게 초기화합니다.

서버·모집 채널·진행 채널·음성채널 ID는 양의 정수로 정규화하며 손상된 값은 미설정 상태로 되돌립니다. 서버 ID와 모집 채널 ID 중 하나만 남은 불완전한 방 연결은 둘 다 해제하므로 관리자가 해당 채널에서 `/내전방생성`을 실행해 다시 연결할 수 있습니다. 한 방의 저장 값이 손상되어도 나머지 정상 방은 유지하며, 확정 근거가 없는 상태만 안전한 대기 상태로 정리합니다.

여러 방이 같은 서버의 같은 모집 채널에 연결돼 있으면 저장 순서상 먼저 복구된 방의 연결만 유지합니다. 이후 방은 참가자·팀·경기 데이터는 보존한 채 서버·모집 채널 연결만 해제되며, 관리자가 빈 채널에서 `/내전방생성`을 실행해 다시 연결할 수 있습니다.

Discord 로그인 완료 후에는 저장된 모집·진행 채널이 실제 일반 텍스트 채널인지, 음성채널 설정이 실제 음성채널인지, 각 채널이 해당 방의 서버에 속하는지 확인합니다. 존재하지 않거나 종류·서버가 다른 설정만 해제합니다. 권한 부족이나 일시적인 Discord 통신 오류로 확인하지 못한 채널은 삭제하지 않고 다음 재시작 때 다시 점검합니다.

다음 명령은 실행 전에 대상 방과 경기번호를 다시 확인합니다.

```text
/경기취소
/경기기록만삭제
/프로필삭제
/전적초기화 확인문구:전체초기화
/내전종료
/내전방삭제
```

- `/전적초기화`는 모든 방이 안전한 상태일 때만 실행됩니다.
- `/내전방삭제`는 참가자와 경기 상태가 없는 빈 방에서만 실행됩니다.
- 데이터 이상이 의심되면 먼저 `/시스템점검`을 실행하고 `data/backups/`의 최신 DB 백업을 확인합니다.

## 9. 배포 전 최종 체크리스트

- [ ] 전체 자동 테스트 통과
- [ ] 봇 시작 시 데이터 경로·DB 무결성·백업 완료 로그 확인
- [ ] Discord에서 `/시스템점검` 결과 확인
- [ ] 3개 방의 모집·진행·음성채널 설정 확인
- [ ] 테스트 참가자로 참가 → 팀 생성 → 경기 → MVP → BO3 종료 확인
- [ ] 재시작 후 방별 참가자·팀·점수 복구 확인
- [ ] 관리자 역할과 봇의 채널 보기·메시지 보내기·링크 첨부·멤버 이동 권한 확인
